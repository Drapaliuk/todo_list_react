const initialState = {
    tasksLists: []
}

export const tasks = (prevState = initialState, action) => {
    switch(action.type)  {
        // case...: 
        //     return {
        //         ...prevState,
        //     }

        default:
            return prevState
    }
}